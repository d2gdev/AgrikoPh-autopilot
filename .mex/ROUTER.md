---
name: router
description: Session bootstrap and navigation hub. Read at the start of every session before any task. Contains project state, routing table, and behavioural contract.
edges:
  - target: context/architecture.md
    condition: when working on system design, integrations, or understanding how components connect
  - target: context/stack.md
    condition: when working with specific technologies, libraries, or making tech decisions
  - target: context/conventions.md
    condition: when writing new code, reviewing code, or unsure about project patterns
  - target: context/decisions.md
    condition: when making architectural choices or understanding why something is built a certain way
  - target: context/setup.md
    condition: when setting up the dev environment or running the project for the first time
  - target: context/data-pipeline.md
    condition: when working on data ingestion, connectors, job handlers, or snapshots
  - target: context/skills-recommendations.md
    condition: when working on AI skills, guardrails, or the recommendation lifecycle
  - target: patterns/INDEX.md
    condition: when starting a task — check the pattern index for a matching pattern file
last_updated: 2026-07-13T13:36:00+08:00
---

# Session Bootstrap

If you haven't already read `AGENTS.md`, read it now — it contains the project identity, non-negotiables, and commands.

Then read this file fully before doing anything else in this session.

## Current Project State

**Working:**
- **SEO inspection-completeness evidence (2026-07-13):** Analysis readiness is independent of actionable gap count. The strict envelope declares required store/link families, required-versus-inspected source counts, and oldest successful timestamps across all relevant inspections, including satisfied/preserve/already-linked observations. Fully inspected zero-gap maps are ready; unknown or missing inspections fail unavailable, and stale/future evidence remains withheld.
- **Candidate-specific SEO observation enforcement (2026-07-13):** Every actionable mapped content/link candidate carries exact store or ArticleRecord link-inspection provenance and capture time, is independently rejected when missing, future-dated, or older than 72 hours, and is revalidated against exact current ArticleRecord state inside the governed proposal transaction. Create targets that now exist, refresh sources that changed/disappeared, and link sources that changed/disappeared/already contain the target return typed conflicts without proposal persistence. Aggregate evidence uses the oldest actionable candidate timestamp per source.
- **Strategy-bound SEO analysis evidence envelope (2026-07-13):** SEO analysis POST/GET share one strict schema-v2 persisted envelope with map gaps isolated from AI presentation, exact active identity, and 72-hour GSC/store/link observation timestamps. Governed blog and link-source handles are directly inspected outside the 200-article presentation cap; non-blog or uninspectable sources fail closed as observation-unavailable. Link candidates require a fresh inspected source and exact normalized destination absence. The UI reloads persisted map analysis before opening Content gaps and distinguishes strategy-identity stale, evidence stale, and unavailable observations.
- **Topical-map command-center local release verification (2026-07-13):** The completed five-job SEO Pilot cutover passed the eight-file focused gate (97/97), full Vitest suite (1,358 passed, 8 skipped), ESLint with zero errors (87 pre-existing warnings), a production Next.js build using only the exact local `autopilot_test` URL with required Prisma pool parameters, and `git diff --check`. Auth-bound route tests prove app authentication precedes Prisma, active identity and all eleven rule domains are projected without source bytes, stale analysis is withheld, exact map-derived content/link proposals retain strategy and rule context, unrelated or stale rule contexts fail closed, and canonical/indexation remain non-executable. Recursive source tests prove the June strategy module/constants, retired panels, and hidden tabs are absent. Production deployment remains pending the required whole-branch review gate; no production, database, environment authorization, Shopify, Meta, or remote-branch mutation occurred during this verification.
- **Topical-map SEO Pilot command center and legacy cutover (2026-07-13):** SEO Pilot now has exactly five operator jobs: map overview, page ownership, governed content gaps, links/technical work, and separately labelled raw search evidence. Map-bound new pages, existing pages with refresh/improve/optimize decisions, and missing-link candidates create governed Content Pilot proposals with exact strategy identity, projected rule domains, rule/source provenance, priority/current GSC evidence, and proposal feedback. Promotion revalidates the exact active rule context before the existing evaluator/compliance/dedupe persistence paths. Redirect persistence is accurately labelled unsupported; canonical and indexation remain advisory-only with live execution prohibited. The static June keyword strategy, old strategy/governance panel composition, unreachable tabs 5–8, and their exclusive promotion/planning/tracking state and handlers are deleted. Observed search evidence, analysis refresh, governed map proposal actions, responsive behavior, and explicit loading/no-strategy/error/stale/empty states remain reachable. Recursive runtime-source regression coverage prevents the legacy constants, module, panels, handlers, and hidden tab branches from returning.
- **Strategy-bound SEO analysis and promotions (2026-07-13):** SEO analysis now joins observations to the active topical-map command-center projection: unmapped GSC demand remains observational, while only mapped missing pages and required absent links become promotable candidates with exact strategy/package/rule identity. Cached analysis uses a versioned envelope and is rejected after strategy identity changes. Gap promotion requires and revalidates that exact active identity and rule set before retaining the existing governed proposal evaluation, compliance, and dedupe transaction paths.
- **Topical-map operator execution cutover (2026-07-13):** The reviewed revision-3 package is the sole active `agrikoph.com` governance strategy. Under verified backups, production physically removed all 406 pre-topical-map non-executed recommendations, 39 unpublished proposals, 385 opportunities, and 288 store tasks. It preserved 22 executed recommendations, 397 published proposals, audit logs, source evidence, and the active strategy. `EXECUTE_APPROVED_LIVE_ENABLED=true` now permits only newly generated recommendations that pass the existing explicit approval/status, permission, guardrail, and topical-map governance boundaries; this is not autonomous execution.
- **Topical-map persisted activation authorization (2026-07-13):** Strategy import now persists contract revision plus safe activation-eligibility projections. Activation and rollback fail closed unless the stored version is valid and both flags are true, including conditional lifecycle claims. The expand-only migration leaves historical rows unauthorized by default and narrowly backfills only the reviewed revision-3 production package; runtime logic contains no package-hash exception. Exact-package identity coverage is hermetic through a committed manifest-only fixture.
- **Topical-map revision 3 production activation (2026-07-13):** The operator-reviewed package `f2a39fabd27a1dcb7ffb29e44695d18a39325186443137dd15762126a8d1bf1c` is the sole active `agrikoph.com` strategy pointer in production. Its activation-authorized contract hash is `3fe3f70b239fc907b61dc8baf96e2c3916c515fd046f2124ea1f2edb0098cb05`; production validation remains `valid` with exactly six artifacts, 1,493 compiled rules, and zero issues. Activation was performed once through the authenticated `SETTINGS_ADMIN` route with actor/reason audit provenance. `TOPICAL_MAP_ACTIVATION_ENABLED=true` enables strategy selection; live execution was initially disabled during rollout and was enabled only after the separately audited legacy-queue cutover. Production backup, migrations, PM2, governance projection, active build, and public health were verified.
- **Topical-map runtime activation gate (2026-07-13):** Production strategy selection now remains fail-closed unless the server-only `TOPICAL_MAP_ACTIVATION_ENABLED` value is exactly `true`. When enabled, the existing validated-version lifecycle transaction, active-pointer race check, and audit persistence remain unchanged. This gate is independent of `EXECUTE_APPROVED_LIVE_ENABLED` and grants no Shopify/Meta live-write authority; production activation still requires separate operator authorization and acceptance evidence.
- **Topical-map strategy package production release (2026-07-13):** Local and production release gates passed for the six-artifact topical-map strategy package. Production was backed up before deployment, all 52 Prisma migrations are current, PM2 and the public health endpoint are healthy, `TOPICAL_MAP_STRATEGY_ROOT` remains unset, and `EXECUTE_APPROVED_LIVE_ENABLED=false` is persisted without an inherited PM2 override. The package remains validation/import-only: no strategy was imported or activated and no Shopify/Meta live write occurred.
- **Bounded Codex agent-loop plan continuation (2026-07-12):** `scripts/codex-agent-loop.mjs` can execute an approved Markdown implementation plan sequentially across finite automatic iteration windows. It validates ordered unique task headings, persists task/window progress, pauses at all existing approval boundaries and the automatic-window ceiling, and publishes sanitized status plus deduplicated task progress events. This is a local development controller only: it does not grant deployment, production, live Shopify/Meta, database, credential, destructive, scope-expansion, or strategy-activation authority. See `patterns/codex-agent-loop.md` for start/resume/status procedure.
- **Topical-map migration safety and operator-validation guidance (Task 11, 2026-07-13):** The local runtime continues to use an operator-triggered validation/import boundary; no topical-map validation cron or job exists. Strategy persistence migrations are expand-only: back up before `prisma migrate deploy`, never use destructive schema rollback, and roll back only by an audited active-pointer transition to an already validated historical version. Import/validation records package evidence but cannot activate a strategy or write to Shopify/Meta. The approved six-artifact package identity `100b4ba60036fc3a93f98fc81964962c564969db03d21613d2aeeac60e57cf5a`, compilation-contract hash `4d1cf3a8583a572ec0e928926ed08057dfdb1fb201b01e177ddb8a35c54b7559`, and contract revision 2 remain validation/import-only; runtime activation now requires the separate exact server-only `TOPICAL_MAP_ACTIVATION_ENABLED=true` gate and still grants no live-execution authority. Local acceptance evidence is required before any separate deployment authority.
- **Topical-map governed links and technical SEO evidence (Task 9, 2026-07-12):** The server-only evaluator loads only Task 8's persisted, active, valid six-artifact projection and returns safe typed proposal/review evidence. Exact normalized internal-link pairs are required; legacy redirect sources and undeclared technical pairs fail closed with rule evidence. Redirect, canonical, and indexation evaluations remain proposal-only (`executionAuthorized: false`) and use persisted freshness plus operator review. Explicit structured medical, dosage, safety, and health classifications carry manual high-stakes review metadata. No activation, approval, drafting, publishing, execution, production, Shopify, Meta, or deployment operation occurred.
- **Topical-map governed Content and SEO proposals (Task 8, 2026-07-12):** The authorized manual Content Pilot and SEO promotion/gap/decomposition proposal paths now evaluate only structured candidate context against an explicitly loaded `agrikoph.com` active pointer inside their proposal transaction. The server-only compliance store accepts only an active, `valid`, complete six-artifact identity with persisted compiled rules and the lossless persisted validator freshness report. It evaluates before a write, records compliant and high-stakes proposals atomically in both `TopicalMapProposalCompliance` and `sourceData.strategyCompliance`, retains high-stakes work as ordinary pending operator review with `executionAuthorized: false`, and stores safe candidate evidence (but no proposal) for conflict/blocked/evidence outcomes. Missing or unavailable strategy/context fails closed. No strategy was activated, no proposal was approved/drafted/published/executed, and no production, Shopify, Meta, or deployment operation occurred.
- **Topical-map secured strategy-package operations (Task 7, 2026-07-12):** Authenticated embedded GET inspection now returns only explicit, source-free Prisma projections for package lifecycle, validation, artifact identity, issue summaries, rule counts, and active-pointer state. `SETTINGS_ADMIN` mutations authenticate and authorize before every parameter/body/environment/filesystem/database/service boundary, import only from the server-configured root with a route-generated UTC `asOf`, and delegate activation/rollback for `agrikoph.com` to Task 5’s transactional audit boundary. Missing configuration, validation failures, lifecycle conflicts, unknown faults, and absent versions return safe typed responses; no UI, cron, evaluator/proposal integration, deployment, production access, or live strategy activation occurred.
- **Topical-map deterministic proposal evaluation (Task 6, 2026-07-12):** A pure evaluator now accepts a caller-supplied compiled active strategy package, six-artifact identity, validator-derived freshness, and normalized candidate context; it never selects a pointer, reads the clock, persists, or executes. It returns stable `compliant`, `conflict`, `blocked`, `needs_evidence`, `needs_high_stakes_review`, or `unavailable_strategy` evidence for content, internal links, redirects, canonicalization, indexation, and SEO metadata. Exact contract rule/source traceability survives every result; stale validator evidence, exclusive owners, explicit no-create, legacy redirect targets, source-condition evidence, and medical/dosage review fail closed. Canonical/indexation/redirect results are proposal-only and never execution authorization. No route, database access, activation, deployment, production access, Shopify/Meta write, or live change occurred.
- **Topical-map activation persistence (Task 5, 2026-07-12):** The local Task 5 implementation adds the sole import/persistence boundary, a lossless nullable validation report, deterministic same-hash coherence checks, and serializable site-scoped activation/supersession/rollback transactions with optimistic pointer ownership and audit provenance. It remains local-only: no route, cron, evaluator, deployment, production access, Shopify/Meta write, or live activation occurred. The guarded local PostgreSQL suite passes 8/8 against `autopilot_test`; topical-map unit/boundary coverage passes 112/112 with both typechecks green.
- **Topical-map whole-package validation (Task 4, 2026-07-12):** A pure server-only validator now cross-checks the six hash-bound raw artifacts, strict contract identity, and complete compiled projection without reading files, consulting time/environment, mutating inputs, or choosing conflict winners. It deterministically reports safe blocking provenance for missing/hash/schema/reference/intent/redirect/canonical conflicts. Every typed declared evidence requirement produces an inspectable gate from the injected UTC `asOf` and manifest evidence date; 180-day general and 90-day high-stakes gates block only when missing/invalid/future or stale. This boundary has no persistence/import execution, activation, API, deployment, production, or Task 5 authority.
- **Topical-map typed evidence-freshness amendment (2026-07-12):** The six-artifact July 12 contract now makes every declared evidence requirement mandatory and explicitly typed: general SEO/market evidence expires after 180 days and high-stakes evidence after 90 days. Validation must use an injected `asOf` timestamp against the manifest `evidenceDate`, block missing/stale mandatory evidence while retaining historical inspection, and emit deterministic `ruleId` + requirement-index gates. Contract revision 2 and package identity `100b4ba60036fc3a93f98fc81964962c564969db03d21613d2aeeac60e57cf5a` are locally approved for validation/import eligibility only; activation, deployment, production access, live Shopify/Meta writes, and Task 5 protected actions remain unauthorized. Semantic Markdown/CSV sources are unchanged.
- **Topical-map whole-package policy compilation (Task 3, 2026-07-12):** Server-only compilation now safely decodes the hash-verified strict contract, requires existing integrity validation before projecting anything, and atomically projects all 1,493 typed rules and 853 disposed coverage units into domain-indexed records. Each record retains contract identity, rule/locator provenance, resolved source coordinates, typed safeguards, and normalized governed URLs without source bytes or prose-derived policy. It rejects source/locator/coverage/ambiguity integrity failures and external governed URLs; it does not persist/import/activate packages, evaluate proposals, expose APIs, or execute any change.
- **Topical-map compilation-contract integrity (Task 2B, 2026-07-12):** Server-only validation now consumes the strict parsed contract plus hash-verified semantic source bytes, resolves every approved anchor, rejects duplicate/dangling/non-bidirectional coverage and rule IDs, undisclosed CSV coverage, typed exclusive-owner conflicts, and unresolved activation-blocking ambiguity with safe typed errors. The approved July 12 package validates as 853 coverage units, 1,493 rules, 163 URL rows, 113 redirect rows, and 456 internal-link rows. It preserves source/rule traceability but does not compile semantics, persist/import/activate packages, access databases or external systems, or authorize Task 3.
- **Topical-map deterministic locator resolution (Task 2B, 2026-07-12):** Server-only resolution now accepts the approved `agriko-locator-v1` Markdown heading/prose-span and CSV-row anchors, confirms their normalized content/header/canonical-row SHA-256 fingerprints from original bytes, and preserves only artifact identity plus line coordinates. It tolerates unique harmless movement, rejects malformed, cross-artifact, missing, ambiguous/duplicated, and drifted anchors through safe typed errors, and resolves the approved July 12 package’s 853 coverage units and 1,493 rules. It does not validate coverage semantics or cross-references, compile rules, import/persist/activate packages, expose source content, or access external systems.
- **Topical-map full compilation-contract grammar (Task 2B parser slice, 2026-07-12):** Server-only parsing now validates the approved strict 1.0.0 contract grammar, including the Task 2A envelope, typed coverage/rules/source locators, strict nested objects, schedule phase-gate fail-closed constraints, advisory schedule/canonical/indexation constraints, and approval metadata. It returns a safe typed schema error without source or contract content. Locator resolution, coverage-reference integrity, package import, persistence, activation, compilation, APIs, and live execution remain out of scope.
- **Topical-map strategy-package manifest validation (Task 2/2A, 2026-07-12):** Server-only Node filesystem/crypto modules now require exactly six artifacts. The `compilation-contract` JSON is raw-byte-hash verified before fatal UTF-8 decoding or JSON parsing, then validates only its fixed `1.0.0` compatibility envelope: revision, strategy version, `agrikoph.com`, ordered five-source hashes, and exact strict compatibility object. Extra top-level fields remain opaque; policy semantics, locators, coverage, approval evidence, and the July 11 contract remain Task 2B. Canonical package identity includes the sixth hash; traversal and root-escaping symlink protection applies equally. Focused fixtures cover all boundary failures without theme files, databases, APIs, LLMs, Shopify state, activation, or deployment.
- **Topical-map strategy-package persistence (Task 1, 2026-07-12):** The isolated Task 1 branch now has an expand-only six-model Prisma contract, immutable package/artifact/rule PostgreSQL triggers, composite foreign keys binding active pointers to their site and compliance records to their package hash, and focused migration/integration tests. PostgreSQL 16 migration and integration verification passed against only local `autopilot_test` after the disposable `test` role was restored with its documented password and required privileges. The integration suite collects `__tests__/integration/**`, not the stale `__tests__/postgres/**` documentation path; the Prisma/PostgreSQL pattern now records that executable contract. No production database, runtime activation, live execution, Shopify write, or deployment occurred.
- **Topical-map strategy package implementation plan (2026-07-12):** `docs/superpowers/plans/2026-07-12-topical-map-strategy-package.md` maps the complete, atomic import and activation design into TDD tasks for immutable storage, validation, deterministic policy evaluation, governed proposal integration, operator workflows, and deployment gates. It does not change runtime behavior.
- **Market Intelligence mutation authorization and refresh integrity (2026-07-11):** Every embedded Market Intelligence mutation now verifies its explicit role before parsing a request, consuming a rate-limit slot, queueing a job, calling AI/translation work, or writing data. Manual captures, keyword research, cache-miss and explicit-refresh AI briefs, ad rewrites, and Content Pilot proposal creation require `CONTENT_REVIEW`; competitor/keyword configuration and the translation backfill require `SETTINGS_ADMIN`. The page now ignores superseded Market overview loads, while forced overview refreshes are actor-rate-limited and share an active aggregation. Focused route regressions cover unauthorized/forbidden short-circuits, rate-limits, and concurrent refresh deduplication.
- **SEO Pilot newer-empty GA4 window correction (2026-07-11):** An explicitly successful newer GA4 snapshot with `topPages: []` now suppresses older normalized reporting windows and reports no current GA4 data, while ambiguous/legacy empty raw payloads continue to preserve usable normalized evidence. Covered by SEO data regressions and full verification; deployed with healthy production evidence.
- **SEO Pilot data-truthfulness follow-up (2026-07-11):** Successful empty GA4 captures now replace normalized rows instead of preserving stale metrics; an existing but empty normalized GA4 window is explicitly labelled `normalized_empty`; prior normalized GSC selection deduplicates reporting windows before applying the 30-window bound; article-backed SEO promotions derive word counts only from canonical `ArticleRecord` data; and Page Health never calls unknown GA4 engagement data a clean result. Covered by SEO data, normalized GSC, route, job, page-health, responsive, and full-suite checks; deployed with healthy production evidence.
- **Embedded Shopify auth recovery (2026-07-11):** Shopify Admin issues App Bridge tokens for the separate session app (`SHOPIFY_SESSION_API_KEY`), so both the browser bundle and server verifier retain that credential pair. Refreshed the session credentials through authenticated Shopify CLI and recreated PM2 with inherited session variables explicitly unset, allowing `dotenv` to load the current `.env` values. Production verified a signed Content Pilot articles request with HTTP 200 and an article array. The expired, ownerless `daily` lock with no active job run was removed through the shared lock helper; production health is now `ok`.
- **SEO Pilot comparison and refresh integrity (2026-07-11):** Period-over-period SEO comparisons now require a strictly earlier, same-duration reporting window across normalized and raw GSC data; overlapping or mismatched ranges produce no false delta. A normalized GSC page load aggregates one database read into query/page/pair views instead of scanning the window three times. Failed section refreshes clear stale data rather than leaving it actionable, and the Keywords action wrapper is Polaris-type-safe. Covered by GSC-window, snapshot, data-load, hook, responsive, route, and full-suite checks.
- **SEO Pilot second surface remediation (2026-07-11):** Article-backed SEO meta rewrites now dedupe to one active mutation per article; deterministic content gaps select eligible uncovered queries before the 30-query bound and prioritize impressions; dashboard refresh polling surfaces only structured failed/partial step names; full GSC source/window/fallback freshness reaches Overview; and Page Health retains and renders simultaneous bounce and conversion findings. Covered by proposal-dedupe, SEO analysis, refresh polling, SEO route, page-health, and responsive-panel regressions.
- **Content Pilot surface-fix reliability pass (2026-07-11):** Queue and draft views now treat publish reconciliation as unresolved, expose durable publication warnings/operation state, and preserve the original generation failure if an authoritative reload also fails. Proposal pagination has truthful totals, validated cursors, tenant-scoped cache keys, bounded request coordination, and no hidden 1,000-row cap; overview article counts load the full paginated corpus without stale refreshes winning. Run Indexer and Content Brief require `CONTENT_REVIEW`, use safe operator errors, and every blog-index entry point now shares the same owner-token job lock. Focused review regressions cover authorization, partial outcomes, pagination, overlapping requests, and lock ownership.
- **SEO Pilot loss/refresh truthfulness pass (2026-07-11):** Tracked keywords that disappear after previously ranking now emit drop alerts; movers use the union of current and previous queries without duplicate rows; missing GA4 rates stay unknown instead of becoming false 0% conversion warnings; zero-upside opportunities are omitted; and Refresh Data follows the returned job run through bounded authenticated polling before reloading results. Covered by focused keyword, trend, page-health, opportunity, and SEO data-hook regressions.
- **SEO Pilot horizontal-overflow removal (2026-07-11):** SEO Pilot navigation now switches from the nine-tab strip to a labelled compact selector below the extra-large breakpoint. All data presentations use a fixed-layout semantic desktop table or labelled stacked records, with long text, links, badges, controls, actions, and sparklines constrained to their container. Opportunities and Keywords retain controlled sorting in both presentations. Covered by `__tests__/components/seo-pilot-responsive.test.ts` across the page shell and all nine panels.
- **SEO Pilot surface-fix state integrity pass (2026-07-11):** AI analysis responses now use the persisted server timestamp and explicitly label deterministic provider fallbacks as partial. Successful keyword writes remain visible if the follow-up report reload fails. SEO core browser cache entries are scoped by Shopify context, SEO history rejects non-SEO snapshot sources, and compact Opportunities/Keywords sort controls reflect the authoritative page sort state. Covered by route, hook, and usability regressions.
- **SEO Pilot surface-fix completeness pass (2026-07-11):** Partial AI analysis retains deterministic missing-meta, thin-content, and internal-link quick wins with evidence. GA4 arbitration ignores empty raw snapshots and treats zero-traffic normalized rows as unusable before falling back to populated raw data. Bulk meta decomposition reads the full article corpus instead of an arbitrary 200-row subset. On-page health returns every offender in the bounded 500-article corpus, maps meta-length findings to the existing SEO-fix action, and labels unsupported findings for manual review.
- **SEO Pilot raw GSC window coherence (2026-07-11):** raw fallback now discards page and query-page snapshots whose reporting window differs from the selected raw query snapshot, preventing stale landing-page attribution. Covered by `__tests__/lib/seo/data.test.ts`.
- **SEO Pilot surface-fix authorization and freshness correction (2026-07-11):** SEO Brief generation, SEO refresh queueing, and tracked-keyword writes now require `CONTENT_REVIEW` immediately after embedded auth; forbidden requests perform no rate-limit, database, job, data, or AI work. Brief provider failures return fixed safe detail instead of raw error text. GA4 raw fallback now identifies an existing normalized window with no usable rows as `normalized_empty`. Covered by SEO route and SEO data regressions.
- **Project-local Codex Surface Fix skill (2026-07-11):** `.codex/skills/surface-fix/SKILL.md` is the single command contract. It defaults to a non-mutating audit; `--fix` enables isolated remediation, verification, and direct merge; `--deploy` implies `--fix` and adds established deployment verification. It forbids partial handoffs and requires explicit approval before any re-review.
- **Content Pilot Task 5 reconciliation/warning correction (2026-07-10)**: a `202` publish response with `reconciliationRequired` is now surfaced as a critical queue error and reloads the authoritative row instead of optimistically marking it published. Scheduled batch reindexing preserves each shared publisher warning, combines it with the batch warning in both cron results and `publishWarning`, and never reports a false clean success. Focused Task 5 suite (54), app/test typechecks, and diff check pass.
- **Content Pilot Task 5 specification completion (2026-07-10)**: reconciliation now uses a proposal-type-specific, read-only Shopify inspector: exact new-content title/body, existing body replacement, SEO metafields, and internal-link operation marker. It returns `applied` only with matching Shopify evidence, resets to `ready` only for a proven `not_applied` existing-target result, and leaves missing/uncertain results ambiguous. Queue filters now surface `publishing` and `publish-error` so the existing Reconcile action is reachable; a `published_with_warnings` response shows the exact operator feedback “Published with warning” plus the stored warning. Covered by focused reconciliation and queue-feedback regressions, app/test typechecks, and diff check.
- **Content Pilot Task 5 finalization completion (2026-07-10)**: The minimal durable Shopify receipt is now written before every post-success SEO/blog lookup; pure resolved existing-article handles are retained on the receipt so follow-up scoring stays eligible. `POST /api/content-pilot/proposals/[id]/retry-bookkeeping` is auth-first plus `CONTENT_PUBLISH` and invokes only the idempotent local finalizer; published rows with an incomplete finalizer show “Retry bookkeeping.” A failed scheduled batch reindex durably writes `publishWarning` on every successfully published row without changing published state. Focused Task 5 suite (43), app/test typechecks, and diff check pass.
- **Content Pilot Task 5 specification recovery follow-up (2026-07-10)**: Shopify-success local SEO/blog-context errors now retain an immediate durable published receipt and return `published_with_warnings`; receipt-write uncertainty is reconciliation-required and is never reset to ready merely because a receipt is missing. Publish actor/trigger are persisted for truthful delayed audit metadata. Scheduled publication performs one batch reindex after per-ID shared publishing. Queue and draft views expose a CONTENT_PUBLISH Reconcile action for unknown/stale publish states. Focused Task 5 suite (36), app typecheck, test typecheck, and diff check pass. See `.superpowers/sdd/task-5-report.md` for red/green evidence.
- **Content Pilot unified publishing (Task 5, 2026-07-10)**: manual and scheduled publication now claim an operation token, re-fetch the owned row before Shopify, persist a durable published receipt before bookkeeping, and finalize Opportunity/audit work idempotently. Post-publish bookkeeping or reindex errors keep the row published with a visible warning; receipt uncertainty is reconciliation-required instead of an unsafe automatic retry. Scheduled work retries up to fifty incomplete finalizers without Shopify. Publish and reconciliation routes use embedded auth followed by `CONTENT_PUBLISH`; scheduled and reindex cron routes use auth then a job lock. Covered by `__tests__/lib/content-pilot/publish-service.test.ts` and publish-failure regressions.
- **Content Pilot atomic proposal transitions (Task 3, 2026-07-10)**: Added nullable operation/lifecycle fields (`draftGenerationToken`, `draftGenerationStartedAt`, `publishOperationId`, `publishStartedAt`, `publishFinalizedAt`, `publishWarning`) and wired `approve/reject/reopen/edit/schedule` through `lib/content-pilot/proposal-transitions.ts` inside `prisma.$transaction`. Transitions now perform a single-write guard+audit+opportunity/history unit, enforce ready/approved/override status gates, and throw typed 409 conflicts when stale state is detected. Covered by `__tests__/lib/content-pilot/proposal-transitions.test.ts`, `__tests__/prisma/content-proposal-operation-state-migration.test.ts`, `__tests__/api/content-pilot-routes.test.ts`, `__tests__/api/content-pilot-reject-route.test.ts`.
- **Content Pilot durable draft ownership (Task 4, 2026-07-10)**: draft generation uses token-based ownership and finalizes inside a conditional transaction against `draftGenerationToken`, preserving one active generation per proposal and preventing stale ownership from overriding fresh work. Claims explicitly include `draftStatus: null` while excluding `generating`/`publishing`; all conditional validation/failure writes return `discarded` when ownership is lost. Receipt preservation is selected atomically by first claiming an actually `published` draft without changing its visible receipt, then conditionally claiming only a non-published draft with `generating`; stale published→ready and ready→published reads therefore cannot hide an active generation or erase a live receipt. Citation collection remains best-effort, but its successful value is persisted in the same token-guarded finalization rather than a later unguarded update. Time-only stale recovery in `GET /api/content-pilot/proposals` was removed in favor of token-aware completion. Covered by `__tests__/lib/content-pilot/generation-service.test.ts`, `__tests__/api/content-pilot-draft-citations.test.ts`, and focused route transition suites.

- **Content Pilot Task 2 mutation matrix and reopen-state normalization (2026-07-10)**: Each of these eleven embedded mutations calls `await requireAppAuth(req)` as its first handler statement, then immediately calls `requirePermission(req, PERMISSIONS.CONTENT_REVIEW)` before any data, AI, Shopify, audit, opportunity-outcome, or draft-history boundary: `POST /api/content-pilot/proposals/[id]/{reject,reopen,clone,generate-draft}`, `PATCH /api/content-pilot/proposals/[id]`, `POST /api/content-pilot/proposals/{generate,manual,refresh-all}`, `POST /api/seo/promote`, `POST /api/seo/gaps/promote`, and `POST /api/seo/recommendations/decompose`. This matrix does not cover publication routes. In particular, `POST /api/content-pilot/regenerate-filipino` remains the mandatory Task 6 remediation and must begin with `CONTENT_PUBLISH`; it is not fixed or deferred by Task 2. Shared pure state helpers recognize `approved` and `override_approved` as the only publishable/generatable statuses, require both that status and a ready draft for edits, and provide the complete reset state for reopening a rejected proposal. PATCH repeats that same approved/override-approved plus ready-draft predicate in its optimistic update guard, so a concurrent rejection or approval revocation returns a conflict before audit/history side effects. The scheduling route applies the same shared predicate before scheduling and repeats it in its optimistic update guard, so stale ready drafts on pending/rejected proposals cannot enter the publish queue. Covered by `__tests__/api/content-pilot-permissions.test.ts`, `__tests__/api/content-pilot-proposal-patch-route.test.ts`, Content Pilot/SEO route suites, and proposal-state tests.
- **Prisma and PostgreSQL verification gates (2026-07-10)**: `npm run db:generate` now writes a hash stamp for `prisma/schema.prisma`, `package.json`, and `package-lock.json`; `npm run verify:prisma-client` fails before typecheck if that generated-client stamp is missing or stale. The default Vitest suite explicitly excludes `__tests__/postgres/**`, so it never needs `DATABASE_URL_TEST`; PostgreSQL integration tests are collected only by `npm run test:postgres`, which reads only `DATABASE_URL_TEST` and accepts only the URL-decoded database name `autopilot_test` on `localhost` or `127.0.0.1` (or the explicit CI `postgres` service host with both opt-ins). CI generates and verifies Prisma before application/test typechecks and provisions PostgreSQL 16 for the isolated suite. Covered by `__tests__/scripts/check-prisma-client.test.ts`, `__tests__/scripts/postgres-test-guard.test.ts`, and `__tests__/scripts/vitest-config.test.ts`.
- **SEO Pilot functional remediation verified (2026-07-10)**: focused suites pass (177 tests), full Vitest passes (906 tests), TypeScript and test typecheck pass, lint has 0 errors, and Prisma validate/build pass with a localhost non-production `DATABASE_URL` (`connection_limit=1`). No production migration was applied. The plan-mandated interactive-transaction `P2002` caveat remains documented: create-first race recovery is implemented where supported, while migration-level and focused route coverage protect the null-safe identity behavior. Migrations `20260710160000_add_content_proposal_dedupe_key` and `20260710161000_market_keyword_null_safe_unique` were reviewed for deterministic canonical/history backfill, child reassignment, and no proposal approval-field mutations.
- **App Bridge near-expiry session-token handling (2026-07-10)**: the browser accepts a freshly issued App Bridge `idToken()` until its actual JWT expiration. The 30-second skew remains a cache-reuse threshold only, so near-expiry tokens are used for the active request but immediately refreshed for the next one instead of producing a false "expired idToken" banner. Covered by `__tests__/hooks/use-auth-fetch.test.ts`.
- **Security and execution safety remediation (2026-07-10)**: embedded browser requests now use Shopify App Bridge bearer tokens exclusively; the server-only `AUTOPILOT_API_KEY` is no longer referenced or bundled by client code. Embedded session-token verification can use `SHOPIFY_SESSION_API_KEY` / `SHOPIFY_SESSION_API_SECRET` independently of the Admin API credential pair, and verification failures log only safe decoded JWT metadata. `executeApprovedHandler()` is dry-run by default and permits live execution only when the caller explicitly requests it and `EXECUTE_APPROVED_LIVE_ENABLED=true` at the job boundary. Git deployment credentials use temporary mode-0700 `GIT_ASKPASS` helpers, with the remote credential delivered over SSH stdin instead of process arguments; deploys retain `.next.old` until a retrying health check succeeds and automatically restore it on failure. The Market Intelligence reset accepts maintenance credentials only in headers and rejects credential-bearing query strings. Covered by focused auth, job/cron, deploy-policy, and reset-route regressions.
- **Plugin review remediation (2026-07-10)**: Content Pilot rejection is now an atomic non-publishable transition (`status`/`draftStatus = rejected`, schedule cleared), and both manual and scheduled publish paths require approved/override-approved status in their optimistic locks. The scheduled publisher now uses the required route-level job lock. Proposal dedupe distinguishes internal-link destinations and structured SEO actions on the same article. Git deploys default to clean local `main`; emergency branches require `--branch <name> --allow-non-main`, SSH host verification stays enabled, remote builds finish before migrations, and PM2 start failure restores `.next.old`. Covered by focused route/dedupe/deploy-policy regressions plus full typecheck and test gates.
- **Content Pilot pre-publish rejection (2026-07-10)**: Content Pilot proposals now remain rejectable until the publishing point. Shared `canRejectContentProposal()` blocks only already-rejected, `publishing`, and `published` proposals. The reject API accepts approved, ready, scheduled, failed, and other pre-publish states with an optimistic update guard; queue rows show a Reject action through those states; the draft review page has a Reject proposal confirmation/note action before publish. Covered by `__tests__/api/content-pilot-reject-route.test.ts`, `__tests__/lib/content-pilot/proposal-state.test.ts`, and `__tests__/components/pilot-usability-helpers.test.ts`.
- **Content Pilot missing Shopify article failure handling (2026-07-10)**: manual and scheduled Content Pilot publish paths now share `contentProposalPublishRecoveryStatus()`. If a refresh/SEO proposal targets an article handle that no longer exists in Shopify, the publish failure moves the proposal to `draftStatus: "failed"` with the Shopify error retained instead of returning it to `ready` and keeping it retryable. Non-idempotent publish failures still use `publish-error`, while ordinary idempotent transient failures can still return to `ready`. Covered by `__tests__/api/content-pilot-publish-failure.test.ts`.
- **SEO/Content Pilot relevance loop hardening (2026-07-10)**: `/api/seo/analyze` now filters AI quick wins and recommendations to items grounded in real article titles/handles, GSC queries, or current on-page issue counts, and returns per-item evidence arrays that the SEO Content Gaps panel renders under every visible strategy bullet. Ungrounded AI bullets are dropped before persistence/display, so unrelated strategy ideas cannot be planned from the SEO UI. SEO recommendation decomposition now dedupes with the shared logical `contentProposalDedupeKey` instead of title-only matching, so rejected/published article actions cannot return under a reworded task title. Content Pilot `refresh-all` now uses `CONTENT_PROPOSAL_RECREATE_BLOCKING_STATUSES`, so rejected guidelines-refresh proposals block bulk recreation. Covered by new regressions in `__tests__/api/seo-pilot-routes.test.ts` and `__tests__/api/content-pilot-routes.test.ts`.
- **SEO/Content Pilot queue usability cleanup (2026-07-10)**: Content Pilot proposal list responses now include `sourceData`, and queue rows render a compact "Why shown" evidence block from persisted source data/proposed state (source, target, score, impressions, position, issue, article handle). Rejected proposal rows now carry an explicit rejected badge. Content Pilot generation and empty states now say when existing/rejected/published/finished ideas are being respected instead of recreated. SEO Pilot content gaps now include an operator-readable reason column, and SEO gap/opportunity empty states no longer imply stale data when the actionable queue is simply clean. Covered by `__tests__/api/content-pilot-routes.test.ts` and `__tests__/components/pilot-usability-helpers.test.ts`.
- **Git-based production deploy path (2026-07-09)**: production `/opt/autopilot` is now a git worktree and the default deploy path is `node scripts/git-deploy.mjs`. The script pushes the current local branch to `origin`, fetches that branch on the VPS using local `GITHUB_TOKEN`, preserves server-owned runtime files (`.env`, `node_modules`, `.next`, `.npm-cache`), runs `npm install`, `npm run db:migrate`, builds into `.next.build`, atomically swaps `.next`, and restarts PM2. Legacy `scripts/linode-deploy.mjs` remains as an rsync fallback only. First git deploy completed successfully on 2026-07-09 with prod health `ok`.
- **SEO/Content Pilot proposal dedupe hardening (2026-07-09)**: shared proposal status/key logic now lives in `lib/content-pilot/proposal-dedupe.ts`. Handle-less `new-content` proposals are keyed by target keyword/title instead of all collapsing to one null-handle bucket, so distinct content gaps can coexist while true duplicates are blocked. `/api/content-pilot/proposals/generate` now filters generated proposals before creating opportunities, preventing stale `Opportunity` rows for proposals skipped as already active; `generateContentOpportunities()` uses the same filter. SEO promotion/decomposition/manual proposal paths now share active status parity (`pending`, `approved`, `override_approved`, `published`), and the SEO Pillar promotion toasts distinguish created vs skipped/no-op outcomes. Covered by `__tests__/api/content-pilot-routes.test.ts`, `__tests__/lib/content-pilot/proposal-dedupe.test.ts`, `__tests__/lib/opportunities/generate.test.ts`, and the existing SEO route suite.
- **SEO Pilot skipped-promotion visibility fix (2026-07-09)**: the SEO Pillar UI now treats any successful `/api/seo/gaps/promote` response with `created > 0` or `skipped > 0` as resolved for the current view. Content gaps and CTR/ranking opportunities are filtered out of their actionable queues after the backend reports "already handled or not promotable", instead of only hiding duplicate skips and leaving missing-article/non-blog-page no-ops visible. Covered by `__tests__/api/seo-pilot-routes.test.ts` regression "removes skipped SEO promote results from the actionable UI queues."
- **Terminal-decision generation dedupe (2026-07-09)**: generated ideas now treat prior operator decisions as recreate blockers instead of checking only the current pending queue. Content proposal generation/direct SEO/manual/opportunity-routing paths use `CONTENT_PROPOSAL_RECREATE_BLOCKING_STATUSES` or replacement-blocking statuses that include `rejected`, so rejected/published content ideas do not come back as new pending work. Skill recommendations and fatigue-derived pause recommendations use `RECOMMENDATION_RECREATE_BLOCKING_STATUSES`, blocking repeat recommendations for the same platform/action/target after pending, approved, executing, executed, or rejected states. Covered by regressions in `__tests__/api/content-pilot-routes.test.ts`, `__tests__/lib/content-pilot/proposal-dedupe.test.ts`, `__tests__/jobs/run-skills.test.ts`, and `__tests__/lib/skills/insight-actions.test.ts`; pattern captured in `.mex/patterns/generation-dedupe.md`.
- **SEO Pilot covered-query gap filtering (2026-07-09)**: `/api/seo/analyze` no longer emits striking-distance GSC queries as net-new `contentGaps` when existing content already covers them. The analysis builder now treats query/page pairs that land on known Shopify blog article handles as covered and also applies majority meaningful-term overlap against existing article titles before suggesting a new article. True uncovered queries still surface as content gaps, while existing-page CTR work remains handled by the Opportunities/promote path as `seo-fix`. Covered by `__tests__/api/seo-pilot-routes.test.ts` regression "does not report already-covered GSC queries as new content gaps."
- **SEO Pilot source-aware GSC freshness selection (Task 1, 2026-07-09)**: `lib/seo/data.ts` now records `LatestGscData.freshness` with selected-source, normalized-window, raw-snapshot, and fallback-reason metadata so downstream SEO consumers can see exactly why a given GSC source was chosen. `getLatestGscData()` still returns the existing `queries`, `pages`, `queryPagePairs`, `fetchedAt`, `source`, and `window` fields, but it now prefers normalized rows only when they exist and the raw `gsc` snapshot is not materially newer than the normalized capture by more than 24 hours. When normalized rows are missing or the raw snapshot is materially fresher, it falls back to raw GSC snapshot payloads and reports `fallbackReason` as `normalized_missing` or `raw_newer_than_normalized` accordingly. Covered by focused Vitest regressions for normalized-preferred, raw-fallback, and no-data metadata cases in `__tests__/lib/seo/data.test.ts`.
- **Organic skill source independence + prioritization (2026-07-09)**: SEO/keyword/content skills now declare required/optional data sources and run from their own base evidence snapshots instead of requiring Meta. `run-skills` refreshes missing/stale required organic sources once per run, records source diagnostics and unavailable-skill reasons in `JobRun.summary`, and organic proposals/opportunities use deterministic scoring for demand, CTR upside, business relevance, confidence, freshness, and effort. Final review follow-up: `lib/skills/runner.ts` now serializes the full stable, deduped effective source union (`requiredSources` + `optionalSources` + legacy `extraSources`) into the LLM payload, and `jobs/run-skills.ts` hashes against that same effective-source path so required/optional-only organic context can no longer pass eligibility but disappear from skip-fingerprints.
- **Growth Brief organic evidence + source-availability surfacing (Task 8, 2026-07-09)**: `app/api/growth-brief/route.ts` now sorts operator queues by normalized priority rank and score evidence instead of priority alone, and the queue fetches for `ContentProposal` / `Opportunity` now use bounded overfetch before the final in-memory trim so operator-visible top items are chosen by queue rank rather than by an incompatible pre-limited DB slice. Organic `ContentProposal` ranking now prefers preserved scorer priority from `sourceData.organicPriority.priority` when present, so clamped UI-facing `priority = "P1"` rows still sort ahead of lower-band proposals when the scorer preserved `P0`. Proposal/opportunity chips still prepend concise evidence strings (`Score`, `Impact`, `Effort`) where that evidence exists. The route also reads the latest `run-skills` `JobRun.summary` and exposes source-aware diagnostics in `dataQuality.runSkills` (`unavailableSources`, unavailable skill count, and the first few missing/stale-source details) without changing generation, execution, or source-registry behavior. The Growth Brief page's Data Quality card renders those run-skills diagnostics as warning badges/text so operators can see when organic work is source-limited. Final review follow-up: routed organic opportunities now preserve `sourceData.organicPriority` at the top level when converting back into `ContentProposal`, instead of leaving it only nested under `sourceData.evidence`, so Growth Brief chips and scorer-band ordering survive routing. Covered by expanded route regressions for over-limit queue selection, preserved organic-priority ordering, same-band score ordering, and the required `growth-brief`, `seo`, `content-pilot`, and `tsc --noEmit` gates.
- **Organic scorer wired into proposals + opportunities (Task 7, 2026-07-09)**: `lib/content-pilot/generate-proposals.ts` now applies `scoreOrganicOpportunity()` to GSC CTR-gap proposals, GSC new-content gaps, and market-intel keyword-gap proposals, persisting the scorer payload under `sourceData.organicPriority` while emitting scorer-derived `priorityScore`, `impact`, and `effort`. Proposal-facing `ContentProposal.priority` stays on the existing UI contract (`P1 | P2 | P3`) by clamping scorer `P0` to `P1`, while `sourceData.organicPriority.priority` preserves the original scored value for audit/routing uses. Non-organic proposal paths keep their existing local scoring. `lib/opportunities/generate.ts` now prefers persisted `sourceData.organicPriority` when turning Content Pilot proposals into Opportunities, so `Opportunity.score` / `priority` / `impact` / `effort` align with the deterministic scorer while the original proposal score is preserved at `evidence.score` for downstream routing/audit uses. `lib/opportunities/route.ts`'s existing `P0 -> P1` ContentProposal mapping stays intact. Covered by proposal/opportunity/router regressions plus the required `generate-proposals`, `content-pilot`, `opportunities`, and `tsc --noEmit` gates.
- **Run-skills source-aware eligibility (Task 4, 2026-07-09)**: `jobs/run-skills.ts` now derives per-skill required/optional/context sources from `SkillDefinition.requiredSources` / `optionalSources` / `primarySource` with the legacy SEO fallback (`platform: "seo"` defaults required sources from `extraSources`). Before dispatch, the job checks required-source freshness through `lib/skills/source-registry.ts`, refreshes any missing/stale required sources once per run, records `summary.sourceStatus` / `summary.sourceRefreshes`, and skips still-unavailable skills into `summary.skillsUnavailable` instead of hard-failing on missing Meta. `platform: "seo"` no longer depends on a Meta snapshot; organic skills select their base snapshot from `selectBaseSnapshotForSource(primarySource)` while meta/both skills stay Meta-backed unless they explicitly declare another primary source. Meta-backed skills that cannot run because the Meta snapshot is absent are now recorded per skill in `summary.skillsUnavailable` with `missingRequiredSources: ["meta"]`, so mixed organic+Meta runs no longer hide Meta unavailability behind unrelated source diagnostics or only a job-level error string. `lib/skills/extra-context.ts` now accepts the full source-contract union and can hydrate snapshot-backed side context for `blog`, `dataforseo_ranked`, `shopify_catalog`, and `shopify_orders` in addition to the existing normalized builders. Covered by a new `run-skills-source-requirements` regression test plus updated run-skills hash/filtering/rotation suites, with hash v2, deferred-hash preservation, and failed/truncated hash removal behavior intentionally unchanged.
- **Organic skill source-contract frontmatter migration (Task 5, 2026-07-09)**: `skills-source/35-google-e2e-seo-assistant.md` and its duplicate `skills-source/seo-pillar/35-google-e2e-seo-assistant.md` explicitly declare `requiredSources: [gsc]`, `optionalSources: [ga4, keyword_research]`, `primarySource: gsc`, `freshnessHours: 96`, and the legacy `extraSources` mirror; the stale in-prompt "platform=seo is non-dispatchable" TODO was removed from the root file. `skills-source/42-google-programmatic-seo-builder.md` and its `seo-pillar/` duplicate now carry the same GSC-first organic contract instead of legacy `platform: SEO` with no explicit source contract. `skills-source/46-google-keyword-gap-analysis.md` declares `platform: "seo"` with `requiredSources: [keyword_research]`, `optionalSources: [gsc, dataforseo_ranked]`, `primarySource: keyword_research`, `freshnessHours: 168`, and preserved `extraSources`. Loader coverage now pins the real repo files for skills `35`, `42`, and `46` so `requiredSources`, `optionalSources`, `primarySource`, and `freshnessHours` regressions fail fast. Ad-adjacent mixed-platform prompts (`10`, `45`) and Meta competitor creative analysis (`13`) were intentionally left unchanged so paid-account skills do not get recast as organic-only work. Verified by `npm test -- loader` and `npm test -- run-skills`.
- **Skill input hash fix + hardening (2026-07-09)**: `jobs/run-skills.ts` no longer uses one Meta-only payload hash for every skill. Each dispatched skill now computes a versioned deterministic pre-AI input fingerprint from `assembleDataPayload(skill, snapshot.payload, extraContextForSkill(skill))` plus skill identity/prompt fields, then uses that value for `JobRun.summary.skillHashes[skill.id]`; dynamic KB grounding/provider output are intentionally excluded from this skip cache. Consequence: Meta-only skills still skip when Meta is unchanged, but skills with `extraSources` rerun when their own GSC/GA4/Market Intelligence/keyword-research context changes even if Meta is unchanged. Existing `skillHashes` / `skillLastRun` summary shapes are preserved; no migration. Deferred skills keep their previous hashes across the 30-skill round-robin cap, while failed/truncated dispatched skills lose stale hashes and do not get a fresh rotation timestamp, so they retry sooner. `lib/skills/extra-context.ts` now adds stable tie-breakers to extra-source ordering so identical persisted data hashes consistently. Covered by focused run-skills tests for GSC-only changes, keyword+GSC changes, market-intel-only changes, prompt edits, unchanged multi-source skips, real `assembleDataPayload()` serialization, deferred-hash preservation, failed-hash removal, and truncated responses removing stale hashes without advancing `skillLastRun`.
- **Dashboard page split (Phase 8b, 2026-07-04)**: `app/(embedded)/page.tsx` reduced from 1,478 to 215 lines by extracting co-located, non-route helper files under `app/(embedded)/components/dashboard/`: `types.ts` (verbatim `JobsStatusPayload` contract move), `helpers.tsx`, `Sparkline.tsx`, `JobHealth.tsx`, `InsightCards.tsx`, `useDashboardData.ts`, plus nine props-only section components under `dashboard/sections/`: `StaleAlertBanner.tsx`, `PendingRecInbox.tsx` (kept in its inviolable lead position), `OperationsRow.tsx`, `PerformanceRow.tsx`, `IntelRow.tsx`, `SkillInsightsSection.tsx`, `JobHealthSection.tsx`, `TrendsSection.tsx`, `RecentActivity.tsx` — page state stays lifted in `page.tsx`. Reused the Phase 8a zero-behavior-change protocol: verbatim moves only, an exhaustive permitted-edit list, an action-inventory table, and hook-count invariance confirmed exactly equal under both a naive and a generic-aware grep pattern. Phase 9 fold-in for this page: all 15 hardcoded hex colors (`stalenessStyle`, `STATUS_DOT_COLOR`, sparkline colors, etc.) replaced with Polaris design tokens — a full zero-raw-hex sweep across `dashboard/` confirms none remain (3 of the 15 initially slipped through across the extraction tasks and were caught and closed by Task 5's own verification pass); the emoji caret in `JobHealth.tsx`'s JobRow was replaced with Polaris `ChevronUpIcon`/`ChevronDownIcon`. The roadmap's other two named offenders for this page — TrendDots' color-only status dots and the caret's keyboard-operability — were checked against the original pre-phase commit and found to already satisfy the a11y requirement (text channel / keyboard operable), so no new work was needed there.
- **Shopify Orders Ingestion (Phase 4, 2026-07-04, docs/gate done — deploy pending operator go-ahead)**: new additive `DailySales` model (migration `20260704020000_daily_sales`, NOT yet applied to prod — no local dev DB either) stores one row per calendar day with revenue as `Float`, not `Decimal` (house precedent for money fields on this project, documented as a deliberate choice, not an oversight). `lib/connectors/shopify-orders.ts` pulls paginated Shopify orders; `jobs/fetch-orders.ts` + `/api/cron/fetch-orders` (04:15 UTC, own job rather than folded into the 01:00 `daily` route because Shopify's day isn't meaningfully complete by 01:00 and that route is already crowded) upserts `DailySales` idempotently per day (`date` is `@unique` — re-running the same day never duplicates), backfills 28 days on first run else just yesterday, excludes cancelled orders from revenue, and snapshots raw pages as `shopify_orders` `RawSnapshot`s. `read_orders` scope was verified once via `scripts/check-order-scopes.ts` (Task 1 hard gate) before any of this was wired up. Dashboard Performance row gained a `revenueVsMeta` card comparing Shopify revenue against Meta ad spend over the same aligned window; `check-outcomes`' KB context gained an advisory (non-blocking) `storeRevenue` field so skill runs can see revenue trend without it affecting outcome verdicts. See `docs/CRON.md` for the job description. **LIVE in prod (verified 2026-07-05)**: migration applied, `/etc/cron.d/autopilot` 04:15 entry installed, and JobRun history shows the 28-day backfill ran 2026-07-04 02:38 (10 orders, ₱5,272 across 28 days) with successful daily runs since — the deploy hold was released earlier than ROUTER previously recorded; a fresh manual acceptance run on 2026-07-05 succeeded (idempotent, 0 orders yesterday).
- **SEO meta write-back (Phase 2, 2026-07-03)**: the article loop was already live (On-Page Health promote → seo-fix `ContentProposal` → AI draft → operator publish → `metafieldsSet` `global.title_tag`/`description_tag` in `publishSeoFix`); this phase added hard length caps at the write boundary (title ≤70, desc ≤320 in `getDraftSchema("seo-fix")`, enforced by `publishDraft`'s re-parse before any Shopify call), a distinct `seo_meta_applied` audit action (vs `published` for other proposal types), "Apply to store" UI labeling for seo-fix drafts, and `updateProductSeo` in `lib/shopify-admin.ts` (`productUpdate` seo input — capability only; no product-targeted seo-fix producer exists yet, so it is intentionally unwired, not dead code to clean up). The roadmap's `apply-seo` route and `updateArticleSeo` were NOT built — both already existed as the publish route and `publishSeoFix` respectively. No schema change. Live acceptance (clicking Apply on a real article) is left to the operator — this phase exercised the write boundary via unit tests only.
- **Operator visibility (Phase 1, 2026-07-03)**: `sendOperatorAlert(kind, payload)` in `lib/alerts.ts` posts typed events (`new_recommendations`, `execution_failed`, `hard_block`, `sla_escalation`, `daily_digest`) to `ALERT_WEBHOOK_URL` — silent no-op when unset, never throws. Event alerts fire from `run-skills.ts` (new recs), `execute-approved.ts` (live-only: execution failures, guardrail hard-blocks), and `ad-approval-sla.ts` (SLA escalations). New `jobs/daily-digest.ts` + `/api/cron/daily-digest` (08:00 UTC) sends a trailing-24h operator digest — pending recs, executions/outcomes, failed jobs, content published, approvals awaiting review. Executed recommendations show outcome verdict badges on the Recommendations page (verdicts: `improved`/`worsened`/`neutral`/`insufficient_data`, via `outcomeTone` in `lib/ui/tones.ts`); dashboard Operations row has an Outcome Win Rate (90d) stat fed by `JobsStatusPayload.outcomeWinRate`, with the card's remainder (neutral/insufficient) shown explicitly to avoid misreading the denominator. No schema change this phase. Alerts stay dormant until an operator sets `ALERT_WEBHOOK_URL` — everything else (digest JobRun recording, badges, win-rate stat) works regardless.
- **Ad-Approvals stepper, timeline, human names (Phase 5, 2026-07-04, read-shaping only — approval state machine NOT touched)**: `lib/ad-approval/stage-progress.ts`'s pure `stageProgress(status, stage)` maps every one of the 15 `AdApprovalStatus` values onto a fixed 7-step stepper (draft → AI pre-review → brand review → conversion review → technical review → penultimate/final approval → approved), exhaustively unit-tested (`Object.values(STATUS) × Object.values(STAGE)`, so a future status addition fails loudly instead of silently rendering nothing); step keys/labels are stable and step count is not hardcoded in the UI, so Phase 7 can append a trailing `launched` step without a rewrite. `lib/ad-approval/timeline.ts`'s `buildApprovalTimeline()` merges three sources server-side into one chronological feed: revision submissions, review decisions, and `AuditLog` rows for the approval. Both `GET /api/ad-approvals/[id]` and `GET /api/ad-approvals` now do an `AppUser` join (by `shopifyUserId`) to build a `names: Record<string, string>` map (`displayName ?? email ?? shopifyUserId`) covering every actor/submitter/reviewer/assignee id referenced, with raw-id fallback when no `AppUser` row exists — `AppUser` is already populated by `captureAppUser` on every authenticated request, so no backfill was needed. The list API additionally enriches each row with `campaignLabel` (resolved from the latest `meta` `RawSnapshot`'s campaign name list, falling back to the raw `campaignId` when the campaign isn't in the latest snapshot — e.g. deleted/renamed campaigns). Both the detail page (`app/(embedded)/(ad-pilot)/ad-approvals/[id]/page.tsx`) and the list page (`app/(embedded)/(ad-pilot)/ad-approvals/page.tsx`) render the stepper/timeline/names — same `names[id] ?? id` fallback pattern in both places, list page accumulates `names` across paginated fetches. No schema change, no new writes, no state-machine edits — purely additive response fields and read-side rendering.
- **Google Ads ad-execution removed; Keyword Planner keyword-research KEPT (2026-07-04, corrected scope)**: The standing directive is "no Google Ads ad spend, ever" — this removed only the already-dead ad-execution/campaign-management code (`SUPPORTED_ACTIONS.google_ads` was always `[]`, nothing has ever executed a Google Ads campaign action): `lib/executor.ts`, `jobs/execute-approved.ts`'s google before-state branch, `jobs/run-skills.ts`'s google_ads dispatch, `lib/recommendations/guardrail-inputs.ts`'s google_ads branch, `jobs/fetch-ads-data.ts`'s opt-in (and always-off) campaign-snapshot step, and 6 skill prompts that were genuinely and permanently about optimizing a live Google Ads ad account (bid strategy, Quality Score, ad extensions, keyword cannibalization, search-term mining, full account audit — deleted from `skills-source/`). Recommendations platform filter/API/badge is Meta-only; `skills/loader.ts`'s `SkillDefinition.platform` union no longer includes `google_ads` for skill-dispatch purposes. **Google Ads Keyword Planner keyword-research is explicitly KEPT** — `lib/connectors/google-ads.ts` and `jobs/fetch-keyword-research.ts` (search volume, competition, bid ranges, long-tail idea discovery + seed auto-promotion into `MarketKeyword`) are live, actively configured (`GOOGLE_ADS_DEVELOPER_TOKEN` etc. set in prod `.env`), and feed real Market Intelligence data — this was mistakenly deleted and fully redeployed once during this session, then identified by the user and restored the same session (see git log 2026-07-04 for the delete→restore commit pair). `KeywordResearchResult.source` defaults to `"google_ads"` again (two migrations: one flipped it to `dataforseo`, a second reverted it — both already applied to prod, left as forward history rather than edited). `connector-health.ts`'s `google_ads_keyword_research` entry, `scripts/google-ads-oauth.mjs`, skill 46's original content, and the CRON/DEVELOPMENT/DEPLOYMENT/OPERATIONS/MARKET_INTELLIGENCE docs are all restored. **If touching Google Ads code again: the connector is keyword-research-only by design — do not wire it into ad execution, and do not delete it.**
- Full daily cron pipeline: fetch-ads-data, fetch-seo-data, fetch-blog-content → run-skills → recommendations
- Operator review UI in Shopify admin (approve/reject/override recommendations)
- execute-approved cron (live execution active in prod via `EXECUTE_APPROVED_LIVE_ENABLED=true`)
- Market intelligence: competitor ads, shopping price history, keyword research (Google Ads Keyword Planner)
- Content Pilot: proposal generation → draft generation → publish to Shopify blog
- ROAS health badges and inline recommendation approval on campaigns page
- Audit quick wins (2026-07-03, items 14–15) + **prod deploy of the full audit batch (Tiers 1–4 + quick wins) via linode-deploy, health check clean**: Odysseus iframe now requires `NEXT_PUBLIC_ODYSSEUS_URL` (no hardcoded fallback URL; unconfigured builds show a banner; loading spinner + open-in-tab fallback; var documented in `.env.example`, set in local `.env` used for prod builds). Content-pilot draft editor: `buildDraftContent()` merges edited fields over the original `draftContent` (previously dropped AI-provided fields outside the form whitelist on every save), Cancel has a discard-confirmation when dirty, saves show a "Draft saved" toast.
- Consistency & IA pass (2026-07-03, audit items 7–12): shared helpers live in `lib/format.ts` (timeAgo/formatPhp/formatMoney/fmtNum/actionLabel) and `lib/ui/tones.ts` (campaign/adApproval/recommendation/priority/severity tone mappers) — new pages must use these, never hand-roll. Loading states use `components/ui/states.tsx` skeletons. Nav is single-sourced from `lib/navigation.ts` (App Bridge menu = `appBridge: true` subset, now covering all pilots) and the Polaris Frame has a TopBar mobile toggle. `/seo` is a client redirect to `/seo-pillar` (AI brief lives on the pillar Overview tab; nav label "SEO" → /seo-pillar). Dashboard leads with the Pending Review inbox. Recommendations/ad-approvals/seo-tables/images have client-side search/filter/sort (content-pilot pattern).
- Silent-failure fixes (2026-07-03, audit Tier 2): Insights/Ad Pilot/Settings/Images pages surface fetch failures as critical Banners with Retry (previously `.catch(() => setLoading(false))` rendered failures as "—"/empty tables; a failed Settings load left Save disabled with no explanation). Images alt-text loop closed end-to-end: `fetchProductImages` now queries the `media` connection (imageId is a MediaImage GID), new `updateProductMediaAlt` + `PATCH /api/images` write alt text to Shopify via `productUpdateMedia` (operator-initiated, audit-logged as `image_alt_text_applied`, rate-limited 30/min, busts the server payload cache), and the Images page shows full-text suggestions with Apply/Copy/Regenerate. Ad-approvals list pages through all records (cap 1000) instead of silently dropping item #101, with a truncation banner when capped.
- Review-action safety UX (2026-07-03, audit Tier 1): Approve on Campaigns + Recommendations opens a shared confirmation modal (`components/ui/approve-confirmation-modal.tsx`) showing the concrete current→proposed change before committing; reject/override modals and Settings credential delete now check `res.ok` and surface failures; approve/reject show a success Toast with Undo backed by a new `POST /api/recommendations/[id]/revert` route (atomic approved/rejected→pending, audit-logged, 409s once the executor claims the rec; override_approved excluded). Also fixed: campaigns reject sent `{reason}` but the API reads `note`, so rejection notes were silently dropped.
- Dashboard v2: ad spend delta, content pilot stats, rec breakdown by action type, collapsible job rows with trend dots + staleness tinting, loading skeletons
- GSC + GA4 data ingestion and SEO pillar dashboard
- Guardrails with DB-configurable thresholds (5-min cache)
- Job locking, health alerts, data freshness monitoring
- Odysseus self-hosted AI workspace at https://odysseus.agrikoph.com — Docker Compose on prod, nginx reverse proxy with Let's Encrypt SSL, embedded as iframe dashboard at `/odysseus` in auto-pilot nav
- Ad Approval workflow (Ad Pilot → Ad Approvals): full state machine, 3 AI review agents (Pre/Brand/Technical, text+HTTP; vision checks stubbed as SKIPPED in v1), Conversion scoring rubric, Penultimate/Final human approval, conflict-of-interest detection, revision history, SLA escalation cron, in-app notifications, Settings reviewer assignment. Code + tests landed and build-clean; migration applied on prod (deploy script runs `migrate deploy` automatically). Async work runs via two new crons: `/api/cron/process-ad-reviews` and `/api/cron/ad-approval-sla` (every 5 min; scheduler is external, see docs/CRON.md).
- DataForSEO Labs organic data (Task 8): `lib/connectors/dataforseo-labs.ts` (`fetchRankedKeywords`, `fetchDomainIntersection`) + a new `jobs/fetch-market-intel.ts` step producing `RawSnapshot` sources `dataforseo_ranked` and `dataforseo_keyword_gap`, plus `keyword_gap` `MarketInsight`s. Independent organic-visibility source while GSC is 403. Gated behind `DATAFORSEO_LABS_ENABLED` (unset/not "true" = fully skipped, no request, no snapshot) — **ENABLED in prod since 2026-07-03** with `DATAFORSEO_LABS_LOCATION=Philippines` (Labs-only location override; live probe 2026-07-03: the PH Labs database has 0 ranked keywords for agrikoph.com vs 31 in US, so `rankedKeywordsFetched` may stay 0 until PH coverage improves — this is data coverage, not a bug; ~$0.012/call, ~4 calls/day). DataForSEO credentials were copied to `/opt/autopilot/.env` 2026-07-03 (previously local-only). The Task 2 `gsc` extra-context fallback chain now extends to `gsc → gsc_query_page → dataforseo_ranked` (`lib/skills/extra-context.ts`). See `docs/MARKET_INTELLIGENCE.md` for field-level detail.
- Recommendation outcome feedback loop: `jobs/check-outcomes.ts` compares before/after connector snapshots for `executed` recommendations 7+ days old, writes the verdict to `Recommendation.outcome`/`outcomeCheckedAt`, and indexes a `recommendation_outcome` KB chunk so future skill runs are grounded in what actually worked. **LIVE in prod since 2026-07-03**: migration `20260702100000_add_recommendation_outcome` applied via deploy (`prisma migrate status`: up to date), cron entry `/api/cron/check-outcomes` installed in `/etc/cron.d/autopilot` at 07:00 daily, and the first manual run succeeded (22 recommendations checked, 9 outcomes indexed, 0 failures). `jobs/index-knowledge.ts`'s orphan-deletion pass is scoped to `INDEXED_SOURCE_TYPES` (`lib/ai/knowledge-sources.ts`) so it never deletes `recommendation_outcome` chunks, which it doesn't own.
- Insights → actions (Phase 3, 2026-07-XX): lib/skills/insight-actions.ts converts fatigue-report insights to pause_ad recommendations (dead 0.9 / urgent 0.7 confidence, dedup vs live recs, guardrail-checked, normal approval pipeline) and refresh-creative StoreTasks (urgent only, dedupeKey store-task:refresh-creative:<adId>); called from run-skills after insight persist, counts feed the new_recommendations alert. Platform "seo" is now dispatchable — skill 46 (keyword-gap-analysis, KEPT per the Google Ads scope rule) runs on keyword_research+gsc extra sources with ad-account payload sections suppressed, finally producing search-term-opportunities insights for the existing opportunities consumer. competitor-analysis insights seed counter-angle new-content ContentProposals inside generateProposals (inside, because the daily cron wipes+regenerates pending proposals nightly).

- **Market Intelligence → Advisory Recommendations (Phase 6, 2026-07-04)**: `lib/market-intel/price-signal.ts` is a pure de-noising layer (7-day median, ±40% outlier rejection, 14-day stability requirement) that now gates the price-gap producer — thresholds are read from `GuardrailConfig` rows `PRICE_GAP_TASK_PCT`/`PRICE_GAP_MIN_DAYS`/`PRICE_OUTLIER_PCT` (defaults 10/14/40), not hardcoded. `keyword_gap` `MarketInsight`s (DataForSEO Labs — a distinct source from the Google Ads Keyword Planner connector, which stays untouched) now seed new-content `ContentProposal`s inside `generateProposals`, closing a gap where they previously had no consumer at all. A zero-capture watchdog fixes the "Falo problem": 7 consecutive empty capture runs for a competitor fire one `competitor_zero_capture` operator alert (alert-once, not daily) plus a persistent `fix_competitor_page` StoreTask. The Market Intelligence page now shows the 7-day smoothed median next to each price comparison — a "Review pricing" badge was considered and explicitly skipped because the product/store→StoreTask linkage wasn't cheaply resolvable without a new unindexed cross-reference join. **Advisory-only invariant held throughout this phase: no code path anywhere in it writes a price** — everything here produces insights, proposals, tasks, and alerts for operator review only.

- **Content-Pilot page split (Phase 8a, 2026-07-04, zero behavior change)**: `app/(embedded)/(content-pilot)/content-pilot/page.tsx` went from 1,820 lines to 277 by extracting co-located components — `components/types.ts` (shared types), `components/helpers.tsx` (shared formatting/badge helpers), `components/OverviewTab.tsx`, `components/BriefTab.tsx`, `components/QueueTab.tsx` (keeps all queue state as the owning orchestrator; children are props-only) with `components/queue/ProposalRow.tsx`, `components/queue/QueueFilters.tsx`, and `components/queue/QueueModals.tsx`. Every extraction task was verified via task-scoped review (one at opus-level effort for the highest-risk QueueTab task) using a zero-behavior-change protocol: verbatim moves only, a 41-row action-inventory table checked line-for-line against the pre-split page, and hook-count invariance confirmed exactly equal under both a literal grep pattern and a broader generic-aware pattern (catches hooks the literal pattern would miss). Phase 9 a11y/theming fold-in for this page was scoped to what actually existed here: one hardcoded hex color replaced with a Polaris design token — the page had no emoji-icons or clickable-non-button elements, so no other a11y work applied. No schema change, no API change, no behavior change; final gate (typecheck/tests/build) re-run clean before merge.
- **SEO-Pillar historical page split (Phase 8c, 2026-07-05; superseded 2026-07-13):** The original 1,176-line page was split into co-located components with zero behavior change and verified action/hook invariance. The later five-tab topical-map command-center cutover superseded the static strategy and hidden legacy panels; only components and handlers reachable from the five current operator jobs remain in the page.

- **A11y & theming sweep (Phase 9, 2026-07-05 — the roadmap's final item)**: raw hex colors replaced with Polaris design tokens on the two remaining offender pages — campaigns (`roasBarColor`, `ConfBar`, with their existing non-color channels — `Badge tone`/"% conf" text — confirmed retained) and recommendations (exec-detail background); emoji-as-icons (▲▼ 💰 ⚠ ✓ ✗) replaced with Polaris icons / `Button disclosure` / plain labels across campaigns, recommendations, and market-intelligence; three scrollable regions made keyboard-focusable (`tabIndex={0} role="region"` + distinct labels: recommendations exec detail, content-pilot raw-JSON pre, dashboard JobHealth error pre — Polaris `Collapsible` unmounts fully-closed children, verified in installed source, so no invisible-focusable region). **The market-intelligence brand hero is the single deliberate no-raw-hex exemption**: its palette is hoisted into a documented `BRAND_HERO` constant (do not "fix" those hexes) and allowlisted in the new policy test `__tests__/a11y/no-raw-hex.test.ts`, which permanently fails CI on any other raw hex under `app/(embedded)`; the hero's muted text alphas were raised 0.68/0.62 → 0.75 for WCAG AA (≈5.6:1) — the phase's one intentional visual change. Browser keyboard-walk was blocked by embedded Shopify auth (307 → `/api/auth/shopify` outside the admin iframe) — recorded, static sweeps clean, tokens are theme-aware by construction. Zero behavior change otherwise; every task gated on the full suite (742/742 incl. the new test) per 8c's process-gap lesson. seo-pillar's lifted promote/plan/track handlers untouched. **Final deploy executed 2026-07-05 on operator go-ahead**: `linode-deploy` clean, health check `ok` with no degraded reasons, `prisma migrate status` up to date, fetch-orders live acceptance succeeded (the `DailySales` backfill turned out to have already run 2026-07-04 — see Phase 4 entry).

**Not yet built / in progress:**
- Social Pilot — page exists but pipeline not fully wired
- Ad Pilot automated creative generation — recommendations exist but creative upload flow incomplete
- Store Pilot — images page and store-pilot page exist; scope TBD

**Known issues:**
- ~~GSC 403 / access blocked~~ RESOLVED (verified 2026-07-03): service account `analytics-ga-4@gen-lang-client-0853027342` is siteOwner on `sc-domain:agrikoph.com`, fetch-seo-data/fetch-gsc-data succeed daily, 13.8k GscQuery rows, live query probe returns real PH rankings. Remaining caveat is genuine low click volume (small site, ~1 click/query) — peso-sizing insights are limited by traffic, not access
- Competitor price stream has noisy data; de-noising is needed before price-based recommendations are reliable
- Legacy direct-analysis routes still use OpenRouter instead of DeepSeek primary — gradual migration ongoing
- `better-sqlite3` competitor scraper DB is local-only; prod market intel uses Serper/DataForSEO instead
- ~~Competitor "Falo" (falo.ph) has no valid numeric Facebook page ID~~ RESOLVED (verified 2026-07-05): Falo's numeric page ID is `206702882526231` — recovered from the Messenger link (`facebook.com/messages/t/<id>`) embedded on falo.ph's own website, and confirmed three ways (graph `/falo.ph/picture` and `/206702882526231/picture` redirect to the identical CDN image; `m.me/falo.ph` redirects to `/msg/206702882526231/`). The prod `CompetitorSocialPage` row already carries this ID (active, set 2026-06-24). A minimal targeted Apify probe (1 page, count=10 — the actor's minimum; "test metered APIs small first") returned **"Ads not found"**: Falo genuinely runs zero Meta ads in PH, so zero captures is the true value, not a config bug. Keep Falo tracked — its shopping/price stream still works, and the ads leg picks up automatically if they ever advertise. **The website-Messenger-link trick is the reliable page-ID recovery method** when Graph lookups fail on permissions (app lacks Page Public Content Access) and anonymous Facebook fetches 400.
- ~~Zero-capture watchdog over-fires~~ FIXED + deployed 2026-07-05: the watchdog in `jobs/fetch-market-intel.ts` now records `summary.apifyRan`, evaluates only immediately after a run that actually executed an Apify pull, and windows over the last 7 *pulls* (`apifyRan === true`, or `apifyAdsFetched > 0` for pre-fix runs) instead of the last 7 daily runs — previously any competitor absent from the single weekly Apify pull tripped the threshold within 8 days (17 false positives on 2026-07-05, every one with a valid numeric pageId; all 17 dismissed on prod with an explanatory completionNote). Task copy now names both causes (misconfigured page ID vs brand genuinely runs no Meta ads). Consequence of the fix: the watchdog needs ~7 weekly pulls of history, so it fires on a ~7-week horizon — correct, since "8 consecutive zero-capture opportunities" is the signal it was always meant to encode.
- Adding a competitor/page with a non-numeric pageId directly via SQL/seed script (bypassing `app/api/market-intelligence/config/route.ts`'s Zod validation) produces a permanently-broken, silent scrape target with no operator-visible error — watch for this pattern when seeding competitors outside the API.

## Routing Table

Load the relevant file based on the current task. Always load `context/architecture.md` first if not already in context this session.

| Task type | Load |
|-----------|------|
| Understanding how the system works | `context/architecture.md` |
| Working with a specific technology | `context/stack.md` |
| Writing or reviewing code | `context/conventions.md` |
| Making a design decision | `context/decisions.md` |
| Setting up or running the project | `context/setup.md` |
| Data ingestion, connectors, job handlers | `context/data-pipeline.md` |
| AI skills, guardrails, recommendations | `context/skills-recommendations.md` |
| Any specific task | Check `patterns/INDEX.md` for a matching pattern |

## Behavioural Contract

For every task, follow this loop:

1. **CONTEXT** — Load the relevant context file(s) from the routing table above. Check `patterns/INDEX.md` for a matching pattern. If one exists, follow it. Narrate what you load: "Loading architecture context..."
2. **BUILD** — Do the work. If a pattern exists, follow its Steps. If you are about to deviate from an established pattern, say so before writing any code — state the deviation and why.
3. **VERIFY** — Load `context/conventions.md` and run the Verify Checklist item by item. State each item and whether the output passes. Do not summarise — enumerate explicitly.
4. **DEBUG** — If verification fails or something breaks, check `patterns/INDEX.md` for a debug pattern. Follow it. Fix the issue and re-run VERIFY.
5. **GROW** — After meaningful work, run this binary checklist:
   - **Ground:** What changed in reality? Name the changed behavior, system, command, dependency, or workflow.
   - **Record:** If project state changed, update the "Current Project State" section above. If documented facts changed, update the relevant `context/` file surgically.
   - **Orient:** If this task can recur and no pattern exists, create one in `patterns/` using `patterns/README.md`, then add it to `patterns/INDEX.md`. If a pattern exists but you learned a gotcha, update it.
   - **Write:** Bump `last_updated` in every scaffold file you changed. If the why matters, run `mex log --type decision "<what changed and why>"` or `mex log "<note>"`.
