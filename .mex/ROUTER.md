---
name: router
description: Session bootstrap and navigation hub. Read at the start of every session before any task. Contains project state, routing table, and behavioural contract.
edges:
  - target: context/architecture.md
    condition: when working on system design, integrations, or understanding how components connect
  - target: context/stack.md
    condition: when working with specific technologies, libraries, or making tech decisions
  - target: context/conventions.md
    condition: when writing new code, reviewing code, or unsure about project patterns
  - target: context/decisions.md
    condition: when making architectural choices or understanding why something is built a certain way
  - target: context/setup.md
    condition: when setting up the dev environment or running the project for the first time
  - target: context/data-pipeline.md
    condition: when working on data ingestion, connectors, job handlers, or snapshots
  - target: context/skills-recommendations.md
    condition: when working on AI skills, guardrails, or the recommendation lifecycle
  - target: patterns/INDEX.md
    condition: when starting a task — check the pattern index for a matching pattern file
last_updated: 2026-07-05T00:00:00Z
---

# Session Bootstrap

If you haven't already read `AGENTS.md`, read it now — it contains the project identity, non-negotiables, and commands.

Then read this file fully before doing anything else in this session.

## Current Project State

**Working:**
- **Dashboard page split (Phase 8b, 2026-07-04)**: `app/(embedded)/page.tsx` reduced from 1,478 to 215 lines by extracting co-located, non-route helper files under `app/(embedded)/components/dashboard/`: `types.ts` (verbatim `JobsStatusPayload` contract move), `helpers.tsx`, `Sparkline.tsx`, `JobHealth.tsx`, `InsightCards.tsx`, `useDashboardData.ts`, plus nine props-only section components under `dashboard/sections/`: `StaleAlertBanner.tsx`, `PendingRecInbox.tsx` (kept in its inviolable lead position), `OperationsRow.tsx`, `PerformanceRow.tsx`, `IntelRow.tsx`, `SkillInsightsSection.tsx`, `JobHealthSection.tsx`, `TrendsSection.tsx`, `RecentActivity.tsx` — page state stays lifted in `page.tsx`. Reused the Phase 8a zero-behavior-change protocol: verbatim moves only, an exhaustive permitted-edit list, an action-inventory table, and hook-count invariance confirmed exactly equal under both a naive and a generic-aware grep pattern. Phase 9 fold-in for this page: all 15 hardcoded hex colors (`stalenessStyle`, `STATUS_DOT_COLOR`, sparkline colors, etc.) replaced with Polaris design tokens — a full zero-raw-hex sweep across `dashboard/` confirms none remain (3 of the 15 initially slipped through across the extraction tasks and were caught and closed by Task 5's own verification pass); the emoji caret in `JobHealth.tsx`'s JobRow was replaced with Polaris `ChevronUpIcon`/`ChevronDownIcon`. The roadmap's other two named offenders for this page — TrendDots' color-only status dots and the caret's keyboard-operability — were checked against the original pre-phase commit and found to already satisfy the a11y requirement (text channel / keyboard operable), so no new work was needed there.
- **Shopify Orders Ingestion (Phase 4, 2026-07-04, docs/gate done — deploy pending operator go-ahead)**: new additive `DailySales` model (migration `20260704020000_daily_sales`, NOT yet applied to prod — no local dev DB either) stores one row per calendar day with revenue as `Float`, not `Decimal` (house precedent for money fields on this project, documented as a deliberate choice, not an oversight). `lib/connectors/shopify-orders.ts` pulls paginated Shopify orders; `jobs/fetch-orders.ts` + `/api/cron/fetch-orders` (04:15 UTC, own job rather than folded into the 01:00 `daily` route because Shopify's day isn't meaningfully complete by 01:00 and that route is already crowded) upserts `DailySales` idempotently per day (`date` is `@unique` — re-running the same day never duplicates), backfills 28 days on first run else just yesterday, excludes cancelled orders from revenue, and snapshots raw pages as `shopify_orders` `RawSnapshot`s. `read_orders` scope was verified once via `scripts/check-order-scopes.ts` (Task 1 hard gate) before any of this was wired up. Dashboard Performance row gained a `revenueVsMeta` card comparing Shopify revenue against Meta ad spend over the same aligned window; `check-outcomes`' KB context gained an advisory (non-blocking) `storeRevenue` field so skill runs can see revenue trend without it affecting outcome verdicts. See `docs/CRON.md` for the job description. **Deploy (migration + cron install + live acceptance) is explicitly deferred to operator go-ahead — do not run `db:migrate` on prod or install the `/etc/cron.d/autopilot` entry until told to.**
- **SEO meta write-back (Phase 2, 2026-07-03)**: the article loop was already live (On-Page Health promote → seo-fix `ContentProposal` → AI draft → operator publish → `metafieldsSet` `global.title_tag`/`description_tag` in `publishSeoFix`); this phase added hard length caps at the write boundary (title ≤70, desc ≤320 in `getDraftSchema("seo-fix")`, enforced by `publishDraft`'s re-parse before any Shopify call), a distinct `seo_meta_applied` audit action (vs `published` for other proposal types), "Apply to store" UI labeling for seo-fix drafts, and `updateProductSeo` in `lib/shopify-admin.ts` (`productUpdate` seo input — capability only; no product-targeted seo-fix producer exists yet, so it is intentionally unwired, not dead code to clean up). The roadmap's `apply-seo` route and `updateArticleSeo` were NOT built — both already existed as the publish route and `publishSeoFix` respectively. No schema change. Live acceptance (clicking Apply on a real article) is left to the operator — this phase exercised the write boundary via unit tests only.
- **Operator visibility (Phase 1, 2026-07-03)**: `sendOperatorAlert(kind, payload)` in `lib/alerts.ts` posts typed events (`new_recommendations`, `execution_failed`, `hard_block`, `sla_escalation`, `daily_digest`) to `ALERT_WEBHOOK_URL` — silent no-op when unset, never throws. Event alerts fire from `run-skills.ts` (new recs), `execute-approved.ts` (live-only: execution failures, guardrail hard-blocks), and `ad-approval-sla.ts` (SLA escalations). New `jobs/daily-digest.ts` + `/api/cron/daily-digest` (08:00 UTC) sends a trailing-24h operator digest — pending recs, executions/outcomes, failed jobs, content published, approvals awaiting review. Executed recommendations show outcome verdict badges on the Recommendations page (verdicts: `improved`/`worsened`/`neutral`/`insufficient_data`, via `outcomeTone` in `lib/ui/tones.ts`); dashboard Operations row has an Outcome Win Rate (90d) stat fed by `JobsStatusPayload.outcomeWinRate`, with the card's remainder (neutral/insufficient) shown explicitly to avoid misreading the denominator. No schema change this phase. Alerts stay dormant until an operator sets `ALERT_WEBHOOK_URL` — everything else (digest JobRun recording, badges, win-rate stat) works regardless.
- **Ad-Approvals stepper, timeline, human names (Phase 5, 2026-07-04, read-shaping only — approval state machine NOT touched)**: `lib/ad-approval/stage-progress.ts`'s pure `stageProgress(status, stage)` maps every one of the 15 `AdApprovalStatus` values onto a fixed 7-step stepper (draft → AI pre-review → brand review → conversion review → technical review → penultimate/final approval → approved), exhaustively unit-tested (`Object.values(STATUS) × Object.values(STAGE)`, so a future status addition fails loudly instead of silently rendering nothing); step keys/labels are stable and step count is not hardcoded in the UI, so Phase 7 can append a trailing `launched` step without a rewrite. `lib/ad-approval/timeline.ts`'s `buildApprovalTimeline()` merges three sources server-side into one chronological feed: revision submissions, review decisions, and `AuditLog` rows for the approval. Both `GET /api/ad-approvals/[id]` and `GET /api/ad-approvals` now do an `AppUser` join (by `shopifyUserId`) to build a `names: Record<string, string>` map (`displayName ?? email ?? shopifyUserId`) covering every actor/submitter/reviewer/assignee id referenced, with raw-id fallback when no `AppUser` row exists — `AppUser` is already populated by `captureAppUser` on every authenticated request, so no backfill was needed. The list API additionally enriches each row with `campaignLabel` (resolved from the latest `meta` `RawSnapshot`'s campaign name list, falling back to the raw `campaignId` when the campaign isn't in the latest snapshot — e.g. deleted/renamed campaigns). Both the detail page (`app/(embedded)/(ad-pilot)/ad-approvals/[id]/page.tsx`) and the list page (`app/(embedded)/(ad-pilot)/ad-approvals/page.tsx`) render the stepper/timeline/names — same `names[id] ?? id` fallback pattern in both places, list page accumulates `names` across paginated fetches. No schema change, no new writes, no state-machine edits — purely additive response fields and read-side rendering.
- **Google Ads ad-execution removed; Keyword Planner keyword-research KEPT (2026-07-04, corrected scope)**: The standing directive is "no Google Ads ad spend, ever" — this removed only the already-dead ad-execution/campaign-management code (`SUPPORTED_ACTIONS.google_ads` was always `[]`, nothing has ever executed a Google Ads campaign action): `lib/executor.ts`, `jobs/execute-approved.ts`'s google before-state branch, `jobs/run-skills.ts`'s google_ads dispatch, `lib/recommendations/guardrail-inputs.ts`'s google_ads branch, `jobs/fetch-ads-data.ts`'s opt-in (and always-off) campaign-snapshot step, and 6 skill prompts that were genuinely and permanently about optimizing a live Google Ads ad account (bid strategy, Quality Score, ad extensions, keyword cannibalization, search-term mining, full account audit — deleted from `skills-source/`). Recommendations platform filter/API/badge is Meta-only; `skills/loader.ts`'s `SkillDefinition.platform` union no longer includes `google_ads` for skill-dispatch purposes. **Google Ads Keyword Planner keyword-research is explicitly KEPT** — `lib/connectors/google-ads.ts` and `jobs/fetch-keyword-research.ts` (search volume, competition, bid ranges, long-tail idea discovery + seed auto-promotion into `MarketKeyword`) are live, actively configured (`GOOGLE_ADS_DEVELOPER_TOKEN` etc. set in prod `.env`), and feed real Market Intelligence data — this was mistakenly deleted and fully redeployed once during this session, then identified by the user and restored the same session (see git log 2026-07-04 for the delete→restore commit pair). `KeywordResearchResult.source` defaults to `"google_ads"` again (two migrations: one flipped it to `dataforseo`, a second reverted it — both already applied to prod, left as forward history rather than edited). `connector-health.ts`'s `google_ads_keyword_research` entry, `scripts/google-ads-oauth.mjs`, skill 46's original content, and the CRON/DEVELOPMENT/DEPLOYMENT/OPERATIONS/MARKET_INTELLIGENCE docs are all restored. **If touching Google Ads code again: the connector is keyword-research-only by design — do not wire it into ad execution, and do not delete it.**
- Full daily cron pipeline: fetch-ads-data, fetch-seo-data, fetch-blog-content → run-skills → recommendations
- Operator review UI in Shopify admin (approve/reject/override recommendations)
- execute-approved cron (live execution active in prod via `EXECUTE_APPROVED_LIVE_ENABLED=true`)
- Market intelligence: competitor ads, shopping price history, keyword research (Google Ads Keyword Planner)
- Content Pilot: proposal generation → draft generation → publish to Shopify blog
- ROAS health badges and inline recommendation approval on campaigns page
- Audit quick wins (2026-07-03, items 14–15) + **prod deploy of the full audit batch (Tiers 1–4 + quick wins) via linode-deploy, health check clean**: Odysseus iframe now requires `NEXT_PUBLIC_ODYSSEUS_URL` (no hardcoded fallback URL; unconfigured builds show a banner; loading spinner + open-in-tab fallback; var documented in `.env.example`, set in local `.env` used for prod builds). Content-pilot draft editor: `buildDraftContent()` merges edited fields over the original `draftContent` (previously dropped AI-provided fields outside the form whitelist on every save), Cancel has a discard-confirmation when dirty, saves show a "Draft saved" toast.
- Consistency & IA pass (2026-07-03, audit items 7–12): shared helpers live in `lib/format.ts` (timeAgo/formatPhp/formatMoney/fmtNum/actionLabel) and `lib/ui/tones.ts` (campaign/adApproval/recommendation/priority/severity tone mappers) — new pages must use these, never hand-roll. Loading states use `components/ui/states.tsx` skeletons. Nav is single-sourced from `lib/navigation.ts` (App Bridge menu = `appBridge: true` subset, now covering all pilots) and the Polaris Frame has a TopBar mobile toggle. `/seo` is a client redirect to `/seo-pillar` (AI brief lives on the pillar Overview tab; nav label "SEO" → /seo-pillar). Dashboard leads with the Pending Review inbox. Recommendations/ad-approvals/seo-tables/images have client-side search/filter/sort (content-pilot pattern).
- Silent-failure fixes (2026-07-03, audit Tier 2): Insights/Ad Pilot/Settings/Images pages surface fetch failures as critical Banners with Retry (previously `.catch(() => setLoading(false))` rendered failures as "—"/empty tables; a failed Settings load left Save disabled with no explanation). Images alt-text loop closed end-to-end: `fetchProductImages` now queries the `media` connection (imageId is a MediaImage GID), new `updateProductMediaAlt` + `PATCH /api/images` write alt text to Shopify via `productUpdateMedia` (operator-initiated, audit-logged as `image_alt_text_applied`, rate-limited 30/min, busts the server payload cache), and the Images page shows full-text suggestions with Apply/Copy/Regenerate. Ad-approvals list pages through all records (cap 1000) instead of silently dropping item #101, with a truncation banner when capped.
- Review-action safety UX (2026-07-03, audit Tier 1): Approve on Campaigns + Recommendations opens a shared confirmation modal (`components/ui/approve-confirmation-modal.tsx`) showing the concrete current→proposed change before committing; reject/override modals and Settings credential delete now check `res.ok` and surface failures; approve/reject show a success Toast with Undo backed by a new `POST /api/recommendations/[id]/revert` route (atomic approved/rejected→pending, audit-logged, 409s once the executor claims the rec; override_approved excluded). Also fixed: campaigns reject sent `{reason}` but the API reads `note`, so rejection notes were silently dropped.
- Dashboard v2: ad spend delta, content pilot stats, rec breakdown by action type, collapsible job rows with trend dots + staleness tinting, loading skeletons
- GSC + GA4 data ingestion and SEO pillar dashboard
- Guardrails with DB-configurable thresholds (5-min cache)
- Job locking, health alerts, data freshness monitoring
- Odysseus self-hosted AI workspace at https://odysseus.agrikoph.com — Docker Compose on prod, nginx reverse proxy with Let's Encrypt SSL, embedded as iframe dashboard at `/odysseus` in auto-pilot nav
- Ad Approval workflow (Ad Pilot → Ad Approvals): full state machine, 3 AI review agents (Pre/Brand/Technical, text+HTTP; vision checks stubbed as SKIPPED in v1), Conversion scoring rubric, Penultimate/Final human approval, conflict-of-interest detection, revision history, SLA escalation cron, in-app notifications, Settings reviewer assignment. Code + tests landed and build-clean; migration applied on prod (deploy script runs `migrate deploy` automatically). Async work runs via two new crons: `/api/cron/process-ad-reviews` and `/api/cron/ad-approval-sla` (every 5 min; scheduler is external, see docs/CRON.md).
- DataForSEO Labs organic data (Task 8): `lib/connectors/dataforseo-labs.ts` (`fetchRankedKeywords`, `fetchDomainIntersection`) + a new `jobs/fetch-market-intel.ts` step producing `RawSnapshot` sources `dataforseo_ranked` and `dataforseo_keyword_gap`, plus `keyword_gap` `MarketInsight`s. Independent organic-visibility source while GSC is 403. Gated behind `DATAFORSEO_LABS_ENABLED` (unset/not "true" = fully skipped, no request, no snapshot) — **ENABLED in prod since 2026-07-03** with `DATAFORSEO_LABS_LOCATION=Philippines` (Labs-only location override; live probe 2026-07-03: the PH Labs database has 0 ranked keywords for agrikoph.com vs 31 in US, so `rankedKeywordsFetched` may stay 0 until PH coverage improves — this is data coverage, not a bug; ~$0.012/call, ~4 calls/day). DataForSEO credentials were copied to `/opt/autopilot/.env` 2026-07-03 (previously local-only). The Task 2 `gsc` extra-context fallback chain now extends to `gsc → gsc_query_page → dataforseo_ranked` (`lib/skills/extra-context.ts`). See `docs/MARKET_INTELLIGENCE.md` for field-level detail.
- Recommendation outcome feedback loop: `jobs/check-outcomes.ts` compares before/after connector snapshots for `executed` recommendations 7+ days old, writes the verdict to `Recommendation.outcome`/`outcomeCheckedAt`, and indexes a `recommendation_outcome` KB chunk so future skill runs are grounded in what actually worked. **LIVE in prod since 2026-07-03**: migration `20260702100000_add_recommendation_outcome` applied via deploy (`prisma migrate status`: up to date), cron entry `/api/cron/check-outcomes` installed in `/etc/cron.d/autopilot` at 07:00 daily, and the first manual run succeeded (22 recommendations checked, 9 outcomes indexed, 0 failures). `jobs/index-knowledge.ts`'s orphan-deletion pass is scoped to `INDEXED_SOURCE_TYPES` (`lib/ai/knowledge-sources.ts`) so it never deletes `recommendation_outcome` chunks, which it doesn't own.
- Insights → actions (Phase 3, 2026-07-XX): lib/skills/insight-actions.ts converts fatigue-report insights to pause_ad recommendations (dead 0.9 / urgent 0.7 confidence, dedup vs live recs, guardrail-checked, normal approval pipeline) and refresh-creative StoreTasks (urgent only, dedupeKey store-task:refresh-creative:<adId>); called from run-skills after insight persist, counts feed the new_recommendations alert. Platform "seo" is now dispatchable — skill 46 (keyword-gap-analysis, KEPT per the Google Ads scope rule) runs on keyword_research+gsc extra sources with ad-account payload sections suppressed, finally producing search-term-opportunities insights for the existing opportunities consumer. competitor-analysis insights seed counter-angle new-content ContentProposals inside generateProposals (inside, because the daily cron wipes+regenerates pending proposals nightly).

- **Market Intelligence → Advisory Recommendations (Phase 6, 2026-07-04)**: `lib/market-intel/price-signal.ts` is a pure de-noising layer (7-day median, ±40% outlier rejection, 14-day stability requirement) that now gates the price-gap producer — thresholds are read from `GuardrailConfig` rows `PRICE_GAP_TASK_PCT`/`PRICE_GAP_MIN_DAYS`/`PRICE_OUTLIER_PCT` (defaults 10/14/40), not hardcoded. `keyword_gap` `MarketInsight`s (DataForSEO Labs — a distinct source from the Google Ads Keyword Planner connector, which stays untouched) now seed new-content `ContentProposal`s inside `generateProposals`, closing a gap where they previously had no consumer at all. A zero-capture watchdog fixes the "Falo problem": 7 consecutive empty capture runs for a competitor fire one `competitor_zero_capture` operator alert (alert-once, not daily) plus a persistent `fix_competitor_page` StoreTask. The Market Intelligence page now shows the 7-day smoothed median next to each price comparison — a "Review pricing" badge was considered and explicitly skipped because the product/store→StoreTask linkage wasn't cheaply resolvable without a new unindexed cross-reference join. **Advisory-only invariant held throughout this phase: no code path anywhere in it writes a price** — everything here produces insights, proposals, tasks, and alerts for operator review only.

- **Content-Pilot page split (Phase 8a, 2026-07-04, zero behavior change)**: `app/(embedded)/(content-pilot)/content-pilot/page.tsx` went from 1,820 lines to 277 by extracting co-located components — `components/types.ts` (shared types), `components/helpers.tsx` (shared formatting/badge helpers), `components/OverviewTab.tsx`, `components/BriefTab.tsx`, `components/QueueTab.tsx` (keeps all queue state as the owning orchestrator; children are props-only) with `components/queue/ProposalRow.tsx`, `components/queue/QueueFilters.tsx`, and `components/queue/QueueModals.tsx`. Every extraction task was verified via task-scoped review (one at opus-level effort for the highest-risk QueueTab task) using a zero-behavior-change protocol: verbatim moves only, a 41-row action-inventory table checked line-for-line against the pre-split page, and hook-count invariance confirmed exactly equal under both a literal grep pattern and a broader generic-aware pattern (catches hooks the literal pattern would miss). Phase 9 a11y/theming fold-in for this page was scoped to what actually existed here: one hardcoded hex color replaced with a Polaris design token — the page had no emoji-icons or clickable-non-button elements, so no other a11y work applied. No schema change, no API change, no behavior change; final gate (typecheck/tests/build) re-run clean before merge.
- **SEO-Pillar page split (Phase 8c, 2026-07-05, zero behavior change)**: `app/(embedded)/(seo-pillar)/seo-pillar/page.tsx` went from 1,176 lines to 558 by extracting co-located files under `components/`: `types.ts`, `brief.tsx`, `widgets.tsx`, `useSeoData.ts`, plus 9 props-only panels under `components/panels/`: `OverviewPanel`, `OpportunitiesPanel`, `ContentGapsPanel`, `OnPageHealthPanel`, `KeywordsPanel`, `PillarClustersPanel`, `PageHealthPanel`, `OpportunityClustersPanel`, `StrategyPanel`. **558 lines is over the 400-line target the 8a/8b page splits hit — a disclosed, reviewed, and approved deviation, not a shortfall in effort**: unlike those two pages, this page's promote/plan/track handlers and their `Set` state are the safety-critical `/api/seo/promote` Phase 2 integration entry point, and the standing rule forbids moving that logic out of the main component, so it (correctly) stayed lifted and accounts for the remaining bulk. Same zero-behavior-change protocol as 8a/8b — verbatim moves only, an action-inventory table, hook-count invariance confirmed exactly equal — plus the per-task hex-ownership upgrade that closes 8b's gap (where 3 of 15 hexes slipped through extraction tasks and had to be caught in a final sweep): here each task owned and closed its own hex conversions as it went. Phase 9 fold-in for this page: 4 Sparkline hex→token swaps + a second (non-color) accessibility channel added to the `Delta` component's arrow indicator; the rest of the page was swept and confirmed clean of emoji-icons and clickable-divs. **Promote-flow semantics are unchanged** — verified via an independent byte-level MD5 diff of the promote handler and its call sites during code review, not just by inspection. Incidentally found and fixed: `__tests__/api/seo-pilot-routes.test.ts` had been silently broken since this phase's Task 1 — an assertion coupled to file layout (an implementation detail) rather than behavior — caught and fixed by this phase's own final-gate verification (commit `332938e`), not a regression introduced by Task 5.

**Not yet built / in progress:**
- Social Pilot — page exists but pipeline not fully wired
- Ad Pilot automated creative generation — recommendations exist but creative upload flow incomplete
- Store Pilot — images page and store-pilot page exist; scope TBD

**Known issues:**
- ~~GSC 403 / access blocked~~ RESOLVED (verified 2026-07-03): service account `analytics-ga-4@gen-lang-client-0853027342` is siteOwner on `sc-domain:agrikoph.com`, fetch-seo-data/fetch-gsc-data succeed daily, 13.8k GscQuery rows, live query probe returns real PH rankings. Remaining caveat is genuine low click volume (small site, ~1 click/query) — peso-sizing insights are limited by traffic, not access
- Competitor price stream has noisy data; de-noising is needed before price-based recommendations are reliable
- Legacy direct-analysis routes still use OpenRouter instead of DeepSeek primary — gradual migration ongoing
- `better-sqlite3` competitor scraper DB is local-only; prod market intel uses Serper/DataForSEO instead
- Competitor "Falo" (falo.ph) has no valid numeric Facebook page ID configured — its `CompetitorSocialPage` row was deactivated by `scripts/seed-competitors.mjs` (non-numeric pageId) and never replaced, so `jobs/fetch-market-intel.ts` silently captures zero ads for it every run. Apify ad-library search and anonymous Facebook scraping couldn't resolve the numeric ID automatically — needs a human to pull it from Facebook's Page Transparency panel and add it via Manage Tracking.
- Adding a competitor/page with a non-numeric pageId directly via SQL/seed script (bypassing `app/api/market-intelligence/config/route.ts`'s Zod validation) produces a permanently-broken, silent scrape target with no operator-visible error — watch for this pattern when seeding competitors outside the API.

## Routing Table

Load the relevant file based on the current task. Always load `context/architecture.md` first if not already in context this session.

| Task type | Load |
|-----------|------|
| Understanding how the system works | `context/architecture.md` |
| Working with a specific technology | `context/stack.md` |
| Writing or reviewing code | `context/conventions.md` |
| Making a design decision | `context/decisions.md` |
| Setting up or running the project | `context/setup.md` |
| Data ingestion, connectors, job handlers | `context/data-pipeline.md` |
| AI skills, guardrails, recommendations | `context/skills-recommendations.md` |
| Any specific task | Check `patterns/INDEX.md` for a matching pattern |

## Behavioural Contract

For every task, follow this loop:

1. **CONTEXT** — Load the relevant context file(s) from the routing table above. Check `patterns/INDEX.md` for a matching pattern. If one exists, follow it. Narrate what you load: "Loading architecture context..."
2. **BUILD** — Do the work. If a pattern exists, follow its Steps. If you are about to deviate from an established pattern, say so before writing any code — state the deviation and why.
3. **VERIFY** — Load `context/conventions.md` and run the Verify Checklist item by item. State each item and whether the output passes. Do not summarise — enumerate explicitly.
4. **DEBUG** — If verification fails or something breaks, check `patterns/INDEX.md` for a debug pattern. Follow it. Fix the issue and re-run VERIFY.
5. **GROW** — After meaningful work, run this binary checklist:
   - **Ground:** What changed in reality? Name the changed behavior, system, command, dependency, or workflow.
   - **Record:** If project state changed, update the "Current Project State" section above. If documented facts changed, update the relevant `context/` file surgically.
   - **Orient:** If this task can recur and no pattern exists, create one in `patterns/` using `patterns/README.md`, then add it to `patterns/INDEX.md`. If a pattern exists but you learned a gotcha, update it.
   - **Write:** Bump `last_updated` in every scaffold file you changed. If the why matters, run `mex log --type decision "<what changed and why>"` or `mex log "<note>"`.
